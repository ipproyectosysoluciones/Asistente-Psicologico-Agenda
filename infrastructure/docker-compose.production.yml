version: '3.8'

# Railway Production Compose
# Nota: PostgreSQL se configura en Railway, no aquí

services:
  n8n:
    image: n8nio/n8n:latest
    container_name: asistente-n8n
    restart: unless-stopped
    environment:
      - N8N_HOST=0.0.0.0
      - N8N_PORT=5678
      - N8N_PROTOCOL=https
      - WEBHOOK_URL=${WEBHOOK_URL}
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=${DB_HOST}
      - DB_POSTGRESDB_PORT=${DB_PORT:-5432}
      - DB_POSTGRESDB_DATABASE=${DB_NAME}
      - DB_POSTGRESDB_USER=${DB_USER}
      - DB_POSTGRESDB_PASSWORD=${DB_PASSWORD}
      - N8N_ENCRYPTION_KEY=${N8N_ENCRYPTION_KEY}
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=${N8N_USER:-admin}
      - N8N_BASIC_AUTH_PASSWORD=${N8N_PASSWORD}
      - EXECUTIONS_MODE=regular
      - EXECUTIONS_TIMEOUT=300
      - EXECUTIONS_DATA_SAVE_ON_ERROR=all
      - EXECUTIONS_DATA_SAVE_ON_SUCCESS=all
      - JWT_SECRET=${JWT_SECRET}
      - DEFAULT_PSYCHOLOGIST_ID=${DEFAULT_PSYCHOLOGIST_ID}
      - N8N_AUTH_USER=${N8N_AUTH_USER}
      - N8N_AUTH_PASS=${N8N_AUTH_PASS}
    volumes:
      - n8n_data:/home/node/.n8n
    ports:
      - "5678:5678"
    networks:
      - asistente-network

  bot:
    image: ${DOCKER_HUB_USERNAME}/asistente-psicologico-bot:latest
    container_name: asistente-bot
    restart: unless-stopped
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - WA_PHONE_NUMBER=${WA_PHONE_NUMBER}
      - HEALTH_PORT=3001
    volumes:
      - bot_sessions:/app/bot_sessions
    networks:
      - asistente-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  dashboard:
    image: ${DOCKER_HUB_USERNAME}/asistente-psicologico-dashboard:latest
    container_name: asistente-dashboard
    restart: unless-stopped
    ports:
      - "80:80"
    networks:
      - asistente-network
    depends_on:
      - n8n
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  n8n_data:
  bot_sessions:
    driver: local

networks:
  asistente-network:
    driver: bridge
