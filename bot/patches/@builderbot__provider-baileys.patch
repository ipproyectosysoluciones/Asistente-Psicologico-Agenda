diff --git a/dist/index.mjs b/dist/index.mjs
index dac44a2fa580945b0849f5586a11a45c9fbc2310..97ae25d2ddab5039760fe544c8765a4494907009 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -33819,30 +33819,6 @@ class BaileysProvider extends ProviderClass {
                     ...this.globalVendorArgs,
                 });
                 this.vendor = sock;
-                if (this.globalVendorArgs.usePairingCode && !sock.authState.creds.registered) {
-                    if (this.globalVendorArgs.phoneNumber) {
-                        const phoneNumberClean = utils$4.removePlus(this.globalVendorArgs.phoneNumber);
-                        const code = await sock.requestPairingCode(this.globalVendorArgs.phoneNumber);
-                        await utils$4.delay(2000);
-                        this.emit('require_action', {
-                            title: '⚡⚡ ACTION REQUIRED ⚡⚡',
-                            instructions: [
-                                `Accept the WhatsApp notification from ${this.globalVendorArgs.phoneNumber} on your phone 👌`,
-                                `The pairing code is: ${code}`,
-                                `Need help: https://link.codigoencasa.com/DISCORD`,
-                            ],
-                            payload: { code },
-                        });
-                    }
-                    else {
-                        this.emit('auth_failure', [
-                            `The phone number has not been defined, please add it`,
-                            `Restart the BOT`,
-                            `You can also check a log that has been created baileys.log`,
-                            `Need help: https://link.codigoencasa.com/DISCORD`,
-                        ]);
-                    }
-                }
                 sock.ev.on('connection.update', async (update) => {
                     const { connection, lastDisconnect, qr } = update;
                     this.logger.log(`[${new Date().toISOString()}] Connection update: ${connection}`);
@@ -33877,8 +33853,30 @@ class BaileysProvider extends ProviderClass {
                     /** Connection opened successfully */
                     if (connection === 'open') {
                         this.logger.log(`[${new Date().toISOString()}] Connection opened successfully`);
-                        this.reconnectAttempts = 0; // Reset counter on successful connection
-                        this.reconnectDelay = 1000; // Reset delay
+                        this.reconnectAttempts = 0;
+                        this.reconnectDelay = 1000;
+                        // Request pairing code now that WebSocket is open
+                        if (this.globalVendorArgs.usePairingCode && !sock.authState.creds.registered) {
+                            if (this.globalVendorArgs.phoneNumber) {
+                                try {
+                                    const code = await sock.requestPairingCode(this.globalVendorArgs.phoneNumber);
+                                    await utils$4.delay(2000);
+                                    this.emit('require_action', {
+                                        title: '⚡⚡ ACTION REQUIRED ⚡⚡',
+                                        instructions: [
+                                            `Accept the WhatsApp notification from ${this.globalVendorArgs.phoneNumber} on your phone 👌`,
+                                            `The pairing code is: ${code}`,
+                                            `Need help: https://link.codigoencasa.com/DISCORD`,
+                                        ],
+                                        payload: { code },
+                                    });
+                                } catch(e) {
+                                    this.logger.log(`[${new Date().toISOString()}] Pairing code request failed: ${e.message}`);
+                                    this.emit('auth_failure', [`Pairing code request failed: ${e.message}`]);
+                                }
+                                return;
+                            }
+                        }
                         const parseNumber = `${sock?.user?.id}`.split(':').shift();
                         const host = { ...sock?.user, phone: parseNumber };
                         this.globalVendorArgs.host = host;
